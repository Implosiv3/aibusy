"""
Module to include the classes that will perform
the operations, so the executor is agnostic.

This will let us make the operations be performed
in different environments (locally, in a docker,
etc.).
"""